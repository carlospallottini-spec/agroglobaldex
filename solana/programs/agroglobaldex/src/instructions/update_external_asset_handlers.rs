//! Stubs for future aggregator updates (toggle handlers re-exported from aggregate.rs).
//! Kept as a placeholder module to avoid breaking external imports.

pub use crate::instructions::aggregate::*;
